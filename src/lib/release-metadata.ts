/**
 * Product build metadata.
 *
 * SemVer lives in package.json. Bump APP_BUILD on each product release to the
 * current merged GitHub PR count at release time.
 */
export const APP_BUILD = "110";
