# Installed via setup.sh with uv (Python 3.12). Kept here for reference —
# magenta-rt[mlx] pins its own heavy dependency tree (mlx, tensorflow, etc.).
#
# Local dev (main.py / `bun run dev:music`) uses the MLX backend + FastAPI.
# Production (handler.py on RunPod Serverless) uses magenta-rt[jax] + runpod;
# see Dockerfile for the container's install list.
numpy>=1.26,<3
magenta-rt[mlx]
fastapi
uvicorn[standard]
python-multipart
# Only needed to run the serverless handler loop locally (`python handler.py`);
# handler() itself imports runpod lazily so unit tests don't require it.
runpod
