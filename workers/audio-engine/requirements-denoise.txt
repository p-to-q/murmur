-r requirements.txt

# Optional denoise stack. Keep this separate from the lightweight worker deps so
# local demos and base CI can run without a large PyTorch install.
deepfilternet==0.5.6
# torch 2.10.0 clears the torch.load RCE (GHSA-53q9-r3pm-6pq6) plus the newer
# unpack_sequence (< 2.9.1) and lstm_cell (< 2.10.0) memory-corruption advisories.
# The torch.jit.script advisory has no patched release yet and is not reachable
# from the DeepFilterNet denoise path. Keep torch/torchaudio versions in lockstep.
torch==2.14.0
torchaudio==2.11.0
