name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    timeout-minutes: 35
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: workers/audio-engine/requirements*.txt

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Playwright Chromium
        run: bunx playwright install --with-deps chromium

      - name: Install audio worker dependencies
        run: |
          cd workers/audio-engine
          python -m venv .venv
          ./.venv/bin/python -m pip install --upgrade pip
          ./.venv/bin/pip install -r requirements.txt

      - name: Install music worker test dependencies
        run: |
          cd workers/music-engine
          python -m venv .venv
          ./.venv/bin/python -m pip install --upgrade pip
          ./.venv/bin/pip install numpy fastapi httpx python-multipart

      - name: Lint
        run: bun run lint

      - name: Typecheck application and tests
        run: |
          bun run typecheck
          bun run typecheck:tests

      - name: Check local markdown links
        run: bun run check:links

      - name: Test
        run: NODE_ENV=test bun test

      - name: Browser golden path
        run: bun run test:e2e

      - name: Audio worker tests
        run: bun run test:audio

      - name: Music worker tests
        run: bun run test:music

      - name: Build audit
        run: bun run build:audit

      - name: Local stack smoke
        run: bash scripts/ci-local-stack-smoke.sh
